import { Action } from './';
export interface ErrorOptions {
    line?: number;
    column?: number;
    moduleId?: string;
    payload?: Object;
}
export interface ErrorAction extends Action {
    title: string;
    message: string;
    line?: number;
    column?: number;
    moduleId?: string;
    payload?: Object;
}
/**
 * Returns an action that describes to show an error
 * in the code of the editor (with the red squiggles)
 *
 * @export
 * @param {string} title
 * @param {string} message
 * @param {ErrorOptions} { line, column, moduleId, payload }
 * @returns {ErrorAction}
 */
export declare function show(title: string, message: string, {line, column, moduleId, payload}: ErrorOptions): ErrorAction;
