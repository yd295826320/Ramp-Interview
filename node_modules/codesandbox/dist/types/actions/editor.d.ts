import { Action } from './';
export interface OpenModuleAction extends Action {
    moduleId: string;
    lineNumber: number;
}
export declare function openModule(id: string, lineNumber?: number): OpenModuleAction;
